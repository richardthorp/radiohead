// For use in the Add Portal Images Post template
// Get the selected file name from the image inputs and render underneath input
$("#lead_image").change(function(){
    let file = $(this)[0].files[0];
    $(this).siblings("strong").children("p").first().text(`Image will be set to ${file.name}`);
});
$("#image_1").change(function(){
    let file = $(this)[0].files[0];
    $(this).siblings("strong").children("p").first().text(`Image will be set to ${file.name}`);
});
$("#image_2").change(function(){
    let file = $(this)[0].files[0];
    $(this).siblings("strong").children("p").first().text(`Image will be set to ${file.name}`);
});
$("#image_3").change(function(){
    let file = $(this)[0].files[0];
    $(this).siblings("strong").children("p").first().text(`Image will be set to ${file.name}`);
});
$("#image_4").change(function(){
    let file = $(this)[0].files[0];
    $(this).siblings("strong").children("p").first().text(`Image will be set to ${file.name}`);
});
$("#image_5").change(function(){
    let file = $(this)[0].files[0];
    $(this).siblings("strong").children("p").first().text(`Image will be set to ${file.name}`);
});
$("#image_6").change(function(){
    let file = $(this)[0].files[0];
    $(this).siblings("strong").children("p").first().text(`Image will be set to ${file.name}`);
});
$("#image_7").change(function(){
    let file = $(this)[0].files[0];
    $(this).siblings("strong").children("p").first().text(`Image will be set to ${file.name}`);
});
$("#image_8").change(function(){
    let file = $(this)[0].files[0];
    $(this).siblings("strong").children("p").first().text(`Image will be set to ${file.name}`);
});